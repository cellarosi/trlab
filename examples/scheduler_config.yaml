jobs:
  - type: market_data_collector
    config:
      symbol: SPY
      data_type: option_chain
      provider: yahoo
      cron_schedule: "* * * * *"
  - type: market_data_collector
    config:
      symbol: AAPL
      data_type: bar
      provider: yahoo
      cron_schedule: "* * * * *"